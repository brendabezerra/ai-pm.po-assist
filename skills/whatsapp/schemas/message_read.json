{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "WhatsApp Message Read",
  "description": "Schema for reading and filtering WhatsApp messages.",
  "type": "object",
  "properties": {
    "input": {
      "type": "object",
      "properties": {
        "filter": {
          "type": "string",
          "enum": ["unread", "recent", "by_chat", "by_group"],
          "default": "unread"
        },
        "chat_name": { "type": "string" },
        "max_messages": { "type": "integer", "default": 20 },
        "since": { "type": "string", "format": "date-time" }
      }
    },
    "output": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "chat_name": { "type": "string" },
          "is_group": { "type": "boolean" },
          "sender": { "type": "string" },
          "message": { "type": "string" },
          "timestamp": { "type": "string", "format": "date-time" },
          "project": { "type": "string" },
          "has_action_item": { "type": "boolean" },
          "summary": { "type": "string" }
        }
      }
    }
  }
}
